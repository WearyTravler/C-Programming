#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <time.h>

int game(char player, char machine) 
{
    // Machine and player both choosing the same choice
    if (player == machine)
        return -1;

    // player
}